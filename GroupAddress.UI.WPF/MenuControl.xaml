<UserControl x:Class="GroupAddress.UI.WPF.MenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GroupAddress.UI.WPF"
             xmlns:vm="clr-namespace:GroupAddress.UI.WPF.ViewModel"
             xmlns:io="clr-namespace:System.IO;assembly=System.IO.FileSystem"
             mc:Ignorable="d"              
             d:DataContext="{d:DesignInstance vm:MenuViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800" x:Name="ThisMenuControl">


    <Menu>
        <MenuItem Header="Datei">
            <MenuItem Header="Neu" InputGestureText="Strg+N" Command="{Binding NewCommand}"/>
            <Separator />
            <MenuItem Header="Beispiel" Command="{Binding OpenSampleProjectCommand}" />
            <MenuItem Header="Datei öffnen" InputGestureText="Strg+O"  Command="{Binding OpenFileCommand}"/>
            <MenuItem Header="Zuletzt geöffnet" ItemsSource="{Binding RecentFilePaths}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
                        <Setter Property="MenuItem.Header" Value="{Binding (io:FileInfo.Name)}"/>
                        <Setter Property="MenuItem.Command" Value="{Binding Path=DataContext.ReadProjectFileCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"/>
                        <Setter Property="MenuItem.CommandParameter" Value="{Binding (io:FileInfo.FullName)}"/>
                        <Setter Property="MenuItem.ToolTip" Value="{Binding (io:FileInfo.FullName)}"/>

                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>
            <Separator />
            <MenuItem Header="Speichern" Command="{Binding SaveCommand}" />
            <MenuItem Header="Speichern unter" Command="{Binding SaveAsCommand}" />
            <Separator />
            <MenuItem Header="Export" />
            <MenuItem Header="Import" />
        </MenuItem>
    </Menu>
</UserControl>
