name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-and-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: src/GroupAddress.Web/package-lock.json

      - name: Install Web dependencies
        run: npm ci
        working-directory: src/GroupAddress.Web

      - name: Lint Web
        run: npm run lint
        working-directory: src/GroupAddress.Web

      - name: Test Web
        run: npm run test
        working-directory: src/GroupAddress.Web

      - name: Restore
        run: dotnet restore GroupAddress.sln

      - name: Build
        run: dotnet build GroupAddress.sln -c Release --no-restore

      - name: Test
        run: dotnet test tests/GroupAddress.Core.Tests/GroupAddress.Core.Tests.csproj -c Release --no-build
